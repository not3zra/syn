# Provider: mock | fallback | groq | fireworks | local
provider: local
# Model override (used when provider=local; MODEL_NAME env var takes precedence):
model: Qwen/Qwen3-8B
# Timeout in seconds for LLM API calls (default: 15.0).
# Bootstrap generation is token-heavy (~2500 tokens) and needs more time.
timeout_seconds: 120.0
